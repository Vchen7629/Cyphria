export type email = {
    auth: {
        email: string
    }
}

export type username = {
    auth: {
        username: string
    }
}

export type userId = {
    auth: {
        userId: string
    }
}